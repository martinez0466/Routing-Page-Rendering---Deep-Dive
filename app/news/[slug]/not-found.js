export default function NewsNotFoundPage() {
    return (
        <div id="error">
            <h1>Not Found!</h1>
            <p>TUnfortunately, we could not find the requested page.</p>
        </div>
    );
}